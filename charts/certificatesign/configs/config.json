{
  "issuers": {
    "default": {
      "publicKey": "{{ .Values.certificatesignenv.CERTIFICATE_PUBLIC_KEY }}",
      "privateKey": "{{ .Values.certificatesignenv.CERTIFICATE_PRIVATE_KEY }}",
      "signatureType": "RSA",
      "verificationMethod": "did:india",
      "$comment": "The above are test keys and it needs to be replaced before going to production"
    }
  }
}