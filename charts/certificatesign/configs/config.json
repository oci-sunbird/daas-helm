{
  "issuers": {
    "default": {
      "publicKey": {{ .Values.certificatesignenv.CERTIFICATE_PUBLIC_KEY | quote }},
      "privateKey": {{ .Values.certificatesignenv.CERTIFICATE_PRIVATE_KEY | quote}},
      "signatureType": "RSA",
      "verificationMethod": "did:india",
      "$comment": "The above are test keys and it needs to be replaced before going to production"
    }
  }
}